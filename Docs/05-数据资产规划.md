# 数据资产规划 — 光标算法训练数据集

> 日期：2026-06-06 | 状态：数据采集阶段

## 目标

积累版本测试数据，未来用于：
- **离线回放测试**：新算法直接在历史手部数据上跑，秒出 23 项指标，不用真人测
- **参数自动调优**：用历史数据做贝叶斯优化/强化学习，自动搜索最优参数
- **模型训练**：手部位置序列 → 理想光标轨迹的端到端模型

## 数据资产清单

| 资产 | 格式 | 位置 | 用途 |
|------|------|------|------|
| 原始日志 | 文本 | `Docs/logs/vX.Y.Z.log` | 完整检测帧数据 |
| 23项指标 | 表格 | `Docs/04-指标历史记录.md` | 版本评分标签 |
| 光标轨迹 | TSV | `Docs/logs/vX.Y.Z-trace.tsv` | 训练用特征 |
| 元数据 | YAML | `Docs/logs/vX.Y.Z-meta.yaml` | 版本参数配置 |

## 日志数据字段

原始日志 `CURSOR-ABS` 行包含：

```
hand=(hx,hy) filter=(fx,fy) target=(tx,ty) cursor=(cx,cy)
```

| 字段 | 含义 | 训练用途 |
|------|------|------|
| handX, handY | palmCenter 原始归一化坐标 | 输入特征 |
| filterX, filterY | 1€ Filter 输出 | 滤波效果标签 |
| targetX, targetY | 映射后的屏幕坐标 | 算法输出 |
| cursorX, cursorY | 120Hz lerp 后的实际光标 | 最终轨迹 |

## 元数据 Schema

```yaml
version: 0.5.0
tag: v0.5.0-silky
date: 2026-06-06
branch: feat/jitter-suppression
git_commit: abc1234
parameters:
  one_euro_filter:
    fc_min: 0.8
    beta: 0.05
    fc_d: 1.0
  lerp: 0.65
  base_gain: 2.0
  calibration_duration: 0.5
  chirality_hysteresis: 3
metrics:  # 23项指标
  total_lag_px: 197
  tc_lag_px: 34
  micro_step_pct: 77.9
  path_efficiency_pct: 89
  jitter_radius_px: 11.8
  y_coverage_pct: 94
  edge_deadzone_pct: 24.1
  direction_misalign_count: 148
  giant_jumps: 5
  frame_drops: 2
  # ... 全部23项
user_rating: "丝滑, 边缘粘滞"
```

## 离线回放测试（已实现 ✅）

```bash
# 方式1: Swift 测试自动回放
swift test  # 自动选trace文件回放，输出到 /tmp/replay-*.log

# 方式2: 代码调用
let frames = ReplayTest.parseHandPositions(from: "Docs/logs/v0.5.0-silky-trace.tsv")
let log = ReplayTest.replay(trace: frames)
// → 写入日志 → ./scripts/analyze-cursor-log.sh 分析

# 方式3: 对比两个版本
git checkout v0.5.0 && swift test  # 测旧算法
git checkout v0.6.1 && swift test  # 测新算法
# 同一trace → 同一分析脚本 → 纯算法差异
```

**原理**：历史 trace 的手部位置 → 当前 CursorController.updateWithAbsolutePosition() → 模拟 120Hz lerp → 输出 CURSOR-ABS 日志 → analyze-cursor-log.sh 分析 23 项指标。

**同一输入、同一分析脚本，指标差异 = 纯算法差异。**

## 数据保存规范

每次测试完成后：

```bash
# 1. 复制日志（不入 git）
cp /tmp/motioncontrol-test.log Docs/logs/vX.Y.Z.log

# 2. 提取轨迹
grep "CURSOR-ABS" Docs/logs/vX.Y.Z.log > Docs/logs/vX.Y.Z-trace.tsv

# 3. 跑分析
./scripts/analyze-cursor-log.sh Docs/logs/vX.Y.Z.log

# 4. 写元数据
# 手动创建 Docs/logs/vX.Y.Z-meta.yaml

# 5. 更新历史记录
# 编辑 Docs/04-指标历史记录.md

# 6. git commit（不含大日志）
```

## 数据量预估

| 指标 | 单次测试 | 100次测试 |
|------|:--:|:--:|
| 日志大小 | 10-70MB | 1-7GB |
| 轨迹行数 | 1000-2000 | 10-20万 |
| 元数据 | 0.5KB | 50KB |

日志不入 git（`.gitignore`），本地归档。轨迹和元数据入 git（几 KB）。
