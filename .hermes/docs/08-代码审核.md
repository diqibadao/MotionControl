# MotionControl 代码审核 — Aider + 人工复核

> 审核方式：Aider ask 模式审查 → 人工复核 → 汇总分类

---

## 审核发现（按模块）

### App 层 — Aider 审核 ✅ 通过

| 文件 | Aider 结论 | 人工确认 |
|------|-----------|---------|
| MotionControlApp.swift | 无问题 | ✅ |
| SystemState.swift | 无问题 | ✅ 但 cameraState/voiceState 用 String 非 enum |
| PermissionManager.swift | 无问题 | ✅ |

### Detection 层 — Aider 审核 发现 6 个问题

| # | 问题 | Aider | 人工 | 文件 |
|---|------|-------|------|------|
| 1 | 阈值单位不匹配（归一化 vs 像素） | ❌ | ❌ | GestureAnalyzer |
| 2 | 手势类型不完整（缺 grab/peace/swipe） | ❌ | ❌ | GestureAnalyzer |
| 3 | GestureType 枚举缺 openPalm/thumbsUp case | ❌ | ✅ 漏了 | GestureConfig |
| 4 | 冷却机制只标 isRepeat 没真抑制 | ❌ | ✅ 漏了 | GestureAnalyzer |
| 5 | velocity 永远 .zero（无跨帧速度） | ❌ | ✅ 漏了 | GestureAnalyzer |
| 6 | GazeEstimator smoothedRawPosition 数据竞争 | ❌ | ✅ 漏了 | GazeEstimator |

### Pipeline + Control 层 — Aider 审核 发现 3 个问题

| # | 问题 | Aider | 人工 | 文件 |
|---|------|-------|------|------|
| 7 | DetectionPipeline 未集成 MouseController | ❌ | ❌ | DetectionPipeline |
| 8 | DetectionPipeline 未集成 GazeEstimator | ❌ | ❌ | DetectionPipeline |
| 9 | CMSampleBufferGetImageBuffer 强制解包 | ⚠️ 没看到 | ❌ | DetectionPipeline |

### Voice 层 — Aider 审核 发现 3 个问题

| # | 问题 | Aider | 人工 | 文件 |
|---|------|-------|------|------|
| 10 | VoiceRecognizerDelegate 与 VoiceInputManager 方法名不一致 | ❌ | ❌ | VoiceRecognizer/VoiceInputManager |
| 11 | TextInjector CGEvent 缺 keyUp，flags 无 Command | ❌ | ✅ 漏了 | TextInjector |
| 12 | VoiceRecognizer 错误后无限重试 | ❌ | ❌ | VoiceRecognizer |

### 全局问题

| # | 问题 | Aider | 人工 | 影响 |
|---|------|-------|------|------|
| 13 | 6 处强制解包 `!` | ⚠️ 提到部分 | ❌ | 高—会 crash |
| 14 | try/catch 比 18:5 | 未检查 | ❌ | 中—错误被吞 |
| 15 | CameraService 默认前置摄像头 | 未检查 | ❌ | 中—C920e 不匹配 |
| 16 | Error 只 print() 不进 UI | 未检查 | ❌ | 低—用户看不到 |

---

## 修复计划

### P0 — 先修（5 项）

| # | 任务 | Aider 指令 |
|---|------|-----------|
| P0-1 | GestureAnalyzer 阈值从归一化改为像素（放大 640 倍） | `--file GestureAnalyzer.swift` |
| P0-2 | DetectionPipeline 集成 MouseController（手势事件→鼠标动作） | `--file DetectionPipeline.swift MouseController.swift` |
| P0-3 | DetectionPipeline 集成 GazeEstimator（注视点→UI） | `--file DetectionPipeline.swift GazeEstimator.swift` |
| P0-4 | 统一 VoiceRecognizerDelegate 方法名 | `--file VoiceRecognizer.swift VoiceInputManager.swift` |
| P0-5 | CMSampleBufferGetImageBuffer 安全解包（guard let） | `--file DetectionPipeline.swift` |

### P1 — 其次修（5 项）

| # | 任务 | Aider 指令 |
|---|------|-----------|
| P1-1 | GestureType 补 openPalm/thumbsUp case | `--file GestureConfig.swift GestureAnalyzer.swift` |
| P1-2 | GestureAnalyzer 冷却真抑制（不返回事件） | `--file GestureAnalyzer.swift` |
| P1-3 | VoiceRecognizer 加错误重试上限（5次停） | `--file VoiceRecognizer.swift` |
| P1-4 | TextInjector CGEvent 补 keyUp + flags | `--file TextInjector.swift` |
| P1-5 | CameraService 默认外接摄像头 | `--file CameraService.swift` |

### P2 — 最后修（4 项）

| # | 任务 |
|---|------|
| P2-1 | 6 处强制解包改为 guard let |
| P2-2 | GazeEstimator smoothedRawPosition 加线程保护 |
| P2-3 | GestureAnalyzer 补 grab/swipe/peace 手势 |
| P2-4 | GestureAnalyzer 阈值读 ConfigManager 而不是硬编码 |
