# MotionControl 技术方案

> 版本：v3.0 更新：2026-06-06 本次更新：绝对位置映射 + 原点校准 + 左右手自动识别 + palmCenter 降级

## 8. 版本历史

| 版本 | 日期 | 技术变更 |
|------|------|---------|
| v7.0 | 2026-06-06 | **离线回放测试框架**（ReplayTest, swift test 自动回放）+ **23 项质量标准 v4.0**（编号化 + 目标校准）+ **数据资产体系**（trace/meta 文件 + 指标历史记录）+ **边缘问题诊断**（死区/方向错位/帧丢失指标） |
| v4.0 | 2026-06-06 | **chirality 修复**（去 X 翻转 + 3 帧滞后锁）+ **响应调优**（1€ Filter beta 0.007→0.05, fcMin 1.0→1.5, lerp 0.5→0.65, 总滞后 328→240px）+ **质量标准体系**（8类14项指标 + 自动分析脚本） |
| v3.0 | 2026-06-06 | **绝对位置映射架构**（增量式→绝对式）+ 原点自动校准（0.5s EMA）+ 左右手自动识别（Vision chirality）+ palmCenter 三级降级（wrist+MCP→MCP→指尖）+ 摄像头镜像方向修正 + pre-commit 门禁系统 |
| v2.0 | 2026-05-31 | 光标算法 v2（Delta 灵敏度+速度基加速曲线+Velocity EMA+方向一致性检查+固定 dt+60fps 补帧）、非对称灵敏度（右/下 1.5x）、激活阈值收紧（0.15）、注视偏移禁用、FaceMeshDetector 双 request（landmarks+faceRects Revision 3 隔15帧）、Dock 磁吸（UIElementScanner）、sub-pixel 移除、30fps 全帧检测 |
| v1.3 | 2026-05-30 | 位置 EMA 替换 velocity 累积、UIElementScanner 新增 Dock 扫描、FaceMeshDetector 新增 VNDetectFaceRectanglesRequest Revision 3、注视偏移在 computeCursor 中注释掉 |
| v1.2 | 2026-05-27 | 头手融合光标计算（CursorController + GazeEstimator）、手势映射配置化（GestureConfig + ConfigManager）、时间序列手势分析（滑动窗口） |
| v1.1 | 2026-05-27 | 手部 21 点检测 + 面部 76 点检测 + CGEvent 光标控制（初始技术方案） |

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                    UI 层 (SwiftUI)                       │
│  CameraPreview  │  CameraOverlay  │  ConfigPanel        │
│  (AVCapture)    │  (手脸点位骨骼)  │  (手势/参数/语音)    │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│                    Control 层                             │
│  ┌─────────────┐  ┌────────────────┐  ┌──────────────┐  │
│  │ MouseCtrl   │  │ KeyboardCtrl   │  │ Accessibility│  │
│  │ (CGEvent)   │  │ (CGEvent)      │  │ Manager      │  │
│  └─────────────┘  └────────────────┘  └──────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│                   Analysis 层                             │
│  ┌───────────┐  ┌──────────┐  ┌───────────┐  ┌───────┐ │
│  │ HandPose  │  │ FaceMesh │  │ Gesture   │  │ Gaze  │ │
│  │ Detector  │  │ Detector │  │ Analyzer  │  │ Estim │ │
│  └─────┬─────┘  └────┬─────┘  └─────┬─────┘  └───┬───┘ │
└────────────────────────┴────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│               Input 层                                    │
│  ┌──────────────┐  ┌──────────────────┐                 │
│  │ CameraService│  │ VoiceRecognizer  │                 │
│  │ (AVCapture)  │  │ (SFSpeech)       │                 │
│  │ VGA 640×480  │  │                  │                 │
│  └──────────────┘  └──────────────────┘                 │
└─────────────────────────────────────────────────────────┘
```

### 分层职责

| 层级 | 职责 | 关键技术 |
| --- | --- | --- |
| **Input** | 原始数据采集 | AVCaptureSession (.vga640x480), AVCaptureVideoDataOutput, SFSpeechRecognizer |
| **Analysis** | CV 检测 + 语义分析 | VNRequest (Vision), HandPoseDetector, FaceMeshDetector, GestureAnalyzer |
| **Control** | 系统级操作 | CGEvent (KeyboardController, MouseController), AccessibilityManager |
| **UI** | 用户界面 | SwiftUI, NSViewRepresentable, CameraPreviewView, CameraOverlayView |

## 2. 核心数据流

### 2.1 每帧处理管道

```
CMSampleBuffer (from AVCapture)
     │
     ├──→ HandPoseDetector.detect()
     │       ↓
     │    handLandmarks[0..20]
     │       ↓
     │    GestureAnalyzer.analyze(landmarks) → gestureType
     │       ↓
     │    MouseController.updatePosition(gestureType, handPosition)
     │
     ├──→ FaceMeshDetector.detect()
     │       ↓
     │    faceLandmarks[0..75]
     │       ↓
     │    ├──→ GazeEstimator.estimate(landmarks)
     │    │       ↓
     │    │    gazePoint (screen coordinate)
     │    │       ↓
     │    │    CursorOverlay.showGazePoint(gazePoint)
     │    │
     │    └──→ VoiceInputManager.checkMouth(landmarks)
     │            ↓
     │         mouthStatus (OPEN / CLOSED)
     │            ↓
     │         VoiceRecognizer.start() / stop()
     │            ↓
     │         MouseController.typeText(text)
     │
     └──→ (optional) Render preview to Metal layer
```

### 2.2 帧内执行顺序（严格）

```
1. AVCaptureVideoDataOutput 回调 → CMSampleBuffer
2. VNSequenceRequestHandler.perform([handPoseRequest, faceRequest])
3. 解析 handPoseRequest.results → handLandmarks
4. 解析 faceRequest.results → faceLandmarks
5. GestureAnalyzer(handLandmarks) → gestureType
6. GazeEstimator(faceLandmarks) → gazePoint
7. MouthDetector(faceLandmarks) → mouthStatus
8. MouseController(gestureType, gazePoint) → CGEvent(s)
9. VoiceInputManager(mouthStatus) → start/stop speech
10. 下一帧
```

**Vision Framework 特点**：多个 VNRequest 可以合并到一次 `perform()` 调用，由 Vision 内部并行化处理，性能更好。

## 3. 核心模块设计

### 3.1 CameraService

```swift
class CameraService: NSObject, AVCaptureVideoDataOutputSampleBufferDelegate {
    private let session = AVCaptureSession()
    private let queue = DispatchQueue(label: "camera.queue", qos: .userInitiated)
    
    func startCapture(deviceID: String? = nil) async throws
    func stopCapture()
    
    // AVCaptureVideoDataOutputSampleBufferDelegate
    func captureOutput(_ output: AVCaptureOutput, 
                      didOutput sampleBuffer: CMSampleBuffer,
                      from connection: AVCaptureConnection)
}
```

- 配置：sessionPreset = .medium (640×480) / .high 取决于性能
- 输出格式：kCVPixelFormatType_32BGRA
- 帧率：30fps，全帧处理（已去掉 %2 跳帧）
- 视频方向：默认 .portrait（摄像头横向取景，需旋转）

### 3.2 HandPoseDetector

```swift
class HandPoseDetector {
    private let request = VNDetectHumanHandPoseRequest()
    
    init() {
        request.maximumHandCount = 1  // 只检测单手
    }
    
    func detect(in pixelBuffer: CVPixelBuffer) throws -> HandPose? {
        let handler = VNSequenceRequestHandler()
        try handler.perform([request], on: pixelBuffer)
        
        guard let observation = request.results?.first,
              let landmarks = try? observation.recognizedPoints(.all) else {
            return nil
        }
        
        return HandPose(observation: observation, landmarks: landmarks)
    }
}
```

### 3.3 FaceMeshDetector

```swift
class FaceMeshDetector {
    private let landmarksRequest = VNDetectFaceLandmarksRequest()
    private let faceRectRequest: VNDetectFaceRectanglesRequest = {
        let r = VNDetectFaceRectanglesRequest()
        r.revision = VNDetectFaceRectanglesRequestRevision3  // 获取连续 yaw/pitch/roll
        return r
    }()
    private var poseFrameCounter = 0
    private var lastRoll: Float?, lastPitch: Float?, lastYaw: Float?
    
    func detect(pixelBuffer: CVPixelBuffer) -> [FaceResult]? {
        // 1. landmarks 请求（每帧都跑，用于面部 76 点和嘴型检测）
        let handler = VNImageRequestHandler(cvPixelBuffer: pixelBuffer, ...)
        try handler.perform([landmarksRequest])
        
        // 2. faceRects 请求（每15帧跑一次，用于头部姿态 yaw/pitch/roll）
        if poseFrameCounter % 15 == 0 {
            let poseHandler = VNImageRequestHandler(cvPixelBuffer: pixelBuffer, ...)
            try poseHandler.perform([faceRectRequest])
            // 更新缓存的姿态值
        }
        // 3. 从 landmarks 取面部 76 点，从缓存取姿态
        return FaceResult(landmarks: ..., roll: lastRoll, pitch: lastPitch, yaw: lastYaw)
    }
}
```

### 3.4 GestureAnalyzer

```swift
class GestureAnalyzer {
    private let config: GestureConfig
    private var lastGesture: GestureType = .none
    private var lastTapTime: Date = .distantPast
    
    func analyze(_ landmarks: HandPose) -> GestureEvent
    
    // 内部判定函数
    private func thumbIndexDistance(_ land: HandPose) -> CGFloat
    private func thumbMiddleDistance(_ land: HandPose) -> CGFloat
    private func fingerExtended(_ land: HandPose, finger: FingerType) -> Bool
    private func isFist(_ land: HandPose) -> Bool
    private func isPeace(_ land: HandPose) -> Bool
}
```

### 3.5 GazeEstimator

```swift
class GazeEstimator {
    private let smoothFactor: Float = 0.4
    private var smoothedGaze: CGPoint?
    
    func estimate(from face: FaceData, screenSize: CGSize) -> CGPoint {
        // 1. 提取眼睑轮廓 → 瞳孔相对位置
        let leftEye = face.landmarks?.leftEye
        let rightEye = face.landmarks?.rightEye
        
        // 2. 计算瞳孔在眼框内的偏移（0~1）
        let leftPupilRatio = pupilOffset(leftEye)
        let rightPupilRatio = pupilOffset(rightEye)
        
        // 3. 结合头部姿态
        let yawFactor = face.yaw / 30.0  // ±30度 → ±1
        let pitchFactor = face.pitch / 30.0
        
        // 4. 映射到屏幕坐标
        let screenX = (0.5 + yawFactor * gazeSensitivity + leftPupilRatio * 0.1) * screenSize.width
        let screenY = (0.5 - pitchFactor * gazeSensitivity) * screenSize.height
        
        // 5. 指数平滑
        let raw = CGPoint(x: screenX, y: screenY)
        smoothedGaze = smoothedGaze.map { 
            CGPoint(
                x: $0.x * (1 - smoothFactor) + raw.x * smoothFactor,
                y: $0.y * (1 - smoothFactor) + raw.y * smoothFactor
            )
        } ?? raw
        
        return smoothedGaze!
    }
}
```

### 3.6 MouseController

```swift
class MouseController {
    func moveCursor(to point: CGPoint) {
        // CGEvent(mouseEventSource: nil, mouseType: .mouseMoved, ...)
        // CGWarpMouseCursorPosition(point)
    }
    
    func leftClick(at point: CGPoint)
    func rightClick(at point: CGPoint)
    func doubleClick(at point: CGPoint)
    func drag(from: CGPoint, to: CGPoint)
    func scroll(delta: CGPoint)
    func typeText(_ text: String)
    func keyPress(_ key: CGKeyCode)
}
```

**辅助功能权限**：CGEvent 需要在 System Settings → Privacy & Security → Accessibility 中授权。

### 3.6 CursorController（2026-05-31 v2 重构）

```swift
class CursorController {
    var currentPosition: CGPoint       // 当前平滑位置
    var isRightHanded: Bool            // 非对称灵敏度方向
    private var smoothVx, smoothVy     // Velocity EMA（α=0.3）
    private let velocityEMAAlpha = 0.3

    func updateWithDelta(tip, lastTip, screenSize, sensitivity, dt) {
        // 1. 计算原始位移（带镜像）
        rawDx = (lastTip.x - tip.x); rawDy = (tip.y - lastTip.y)
        // 2. 非对称灵敏度（右/下方向 1.5x）
        // 3. 死区 < 2px 忽略
        // 4. Velocity EMA 平滑
        // 5. 方向一致性检查（raw×smooth≤0时清零，防画十字漂移）
        // 6. 加速曲线（vel<50→0.4x, <200→1x, ≥200→1~4x）
        // 7. 固定 dt=1/30（velocity 不受帧间隔抖动影响）
        currentPosition += smoothDelta * factor
    }
    
    var displayVelocityX/Y: CGFloat { smoothVx/smoothVy }  // 60fps 补帧用
}
```

**算法链：**
```
Vision indexTip → delta × 屏幕 × 灵敏度(3)
  → 非对称 1.5x（右/下方向）
  → 死区 < 2px → Velocity EMA(α=0.3)
  → 方向一致性检查 → 加速曲线
  → 固定 dt = 1/30 → 60fps Timer 补帧输出
```

### 3.7 VoiceRecognizer

```swift
class VoiceRecognizer: NSObject, SFSpeechRecognizerDelegate {
    private let recognizer: SFSpeechRecognizer?
    private var recognitionTask: SFSpeechRecognitionTask?
    private let audioEngine = AVAudioEngine()
    
    func startRecording() throws
    func stopRecording() -> String
    func requestAuthorization() async -> Bool
}
```

### 3.8 CursorOverlay

- 独立 NSWindow（浮层模式）
- 不接收焦点（ignoresMouseEvents = true）
- level = .floating
- 在 NSView 中用 CGContext 或 Metal 绘制手势光标和注视光标
- 双光标样式区分：
  - 手势光标：圆形 + 颜色变化（绿色→正常，红色→点击）
  - 注视光标：半透明十字准星或瞄准环

## 4. 关键技术决策

### 4.1 为什么用 Vision Framework 而不是 MediaPipe？

| 方面 | Vision Framework | MediaPipe |
| --- | --- | --- |
| 审核合规 | ✅ 系统框架，无需额外说明 | ❌ 需要声明第三方库 |
| 性能 | ✅ 系统优化，Apple Silicon 加速 | ⚠️ 通用框架 |
| 包体积 | ✅ 零体积 | ❌ 约 10-30MB |
| 集成难度 | ✅ 直接调用 | ⚠️ 需要 C++ 桥接或 Pod |
| 手势精度 | ✅ 21 两点足够 | ✅ 21 点相同 |

### 4.2 为什么用 CGEvent 而不是 Accessibility API？

| 方面 | CGEvent | Accessibility API |
| --- | --- | --- |
| 鼠标控制 | ✅ 精确坐标 + 左/右/中键 | ✅ 支持 |
| 键盘控制 | ✅ 支持 | ✅ 支持 |
| 文本输入 | ✅ CGEventPostKeyboardEvent | ❌ 不支持 |
| 响应速度 | ✅ 系统级，快 | ⚠️ 需要 IPC |
| 权限要求 | ⚠️ 辅助功能 | ⚠️ 辅助功能 |

**决策**：CGEvent 更底层、更灵活，文本输入也必须用 CGEvent。

### 4.3 检测频率策略

- Vision 推理最耗时，不可每帧都跑
- 策略：渲染帧（显示预览）和检测帧（手部/面部推理）分离
- 检测帧率：30fps（全帧处理，已去掉 %2 跳帧）
- 输出帧率：60fps（Timer 补帧，两帧检测之间插 1 帧）
- ⚠️ 已知问题：当前手部+面部检测在同一个 serial queue，face 排队阻塞 hand（需拆为 handQueue + faceQueue）

## 5. 线程模型

```
主线程 (UI)         检测线程         控制线程
    │                  │                │
    │    ┌─────────────┼─────┐          │
    │    │ AVCapture    │     │          │
    │    │ VideoData    │     │          │
    │    │ Output 回调   │     │          │
    │    └──────┬───────┘     │          │
    │           │             │          │
    │    ┌──────┴───────┐     │          │
    │    │ VNRequest     │     │          │
    │    │ Perform()     │     │          │
    │    └──────┬───────┘     │          │
    │           │             │          │
    │    ┌──────┴───────┐     │          │
    │    │ 分析结果回调    │     │          │
    │    └──────┬───────┘     │          │
    │           │             │          │
    │    DispatchQueue.main  │          │
    │    .async {            │          │
    │      UI 更新           │          │
    │      CGEvent 调用      │          │
    │    }                   │          │
```

- **检测线程**（目前问题）：`processingQueue` 串行队列，手部+面部检测排队执行，face 排队阻塞 hand
- ⚠️ **待修复**：拆为 `handQueue` + `faceQueue` 两个独立队列，手部 30fps 独立运行
- **控制线程**：CGEvent 必须在主线程或同一线程连续执行（这里放主线程 + 60fps Timer 补帧）
- **UI 线程**：SwiftUI 更新

## 6. 依赖关系图

```
CameraService ←────────── HandPoseDetector ←── GestureAnalyzer
                ↕                    ↕
           FaceMeshDetector ←── GazeEstimator (已禁用注视偏移)
                     ↕
              MouthDetector
                     ↕
           DetectionPipeline (调度层)
           ├── onHandResult → CursorController
           │                       ↕
           │             MouseController (30fps 检测帧 + 60fps 补帧)
           │                       ↕
           │                 CGWarpMouseCursorPosition
           └── onFaceResult → CameraOverlayView (76 点绘制)

UIElementScanner (后台 500ms 轮询 AX API)
           ↕
      CursorController.magnetState (磁性吸附)
```

所有模块依赖 `GestureConfig`（配置单例），`CameraService` 是数据源，`MouseController` 和 `KeyboardController` 是终端输出。

---

## 9. 质量评估标准

参见 [03-光标跟手质量标准.md](03-光标跟手质量标准.md)。

### 核心参考

| 标准/模型 | 应用 |
|------|------|
| ISO 9241-9/411 (Fitts' Law) | 指向设备 Throughput 评估 |
| MacKenzie 轨迹质量模型 | MDC/ODC/TAC/路径效率 |
| CMU 手部跟踪研究 | 静止抖动基准 |
| Kjeldsen 滞后-抖动模型 | 延迟对小目标选择的指数级影响 |

### 分析工具

```bash
./scripts/analyze-cursor-log.sh /tmp/motioncontrol-test.log
```

输出综合评分 + 8 类 14 项指标 + 基准对比。